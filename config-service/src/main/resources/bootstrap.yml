server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wlopera/java-micro-services.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "micro-service"
  security:
    user:
      name: root
      password: s3cr3t 